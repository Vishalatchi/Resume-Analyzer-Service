This is a Python Service developed to analyze the candidate resume using Groq AI.
It accepts resume in pdf format.
It uses FastAPI for service development.
Groq AI incorporated for resume evaluation.
Infisical key vault used to store Groq AI API key.
pydantic schema validation included to ensure the data correcness.
logging mechanism included.
middleware enabled for logging the service request and response details.
AI Api retry and timeout mechanism included to avoid resilience and stability.
environment file maintain to store keys.
Proper folder structure followed as much as possible as per industry standards.
